控制继承：
inherit:
inherit属性会使子元素属性和父元素属性相同
适用范围：可以应用于任何css属性

initial：
将属性的初始（或默认）值应用于元素

revert：
将应用于选定元素的属性值重置为浏览器的默认样式

revert-layer：
将应用于选定元素的属性值重置为上一个层叠层建立的值

unset：
属性重置为自然值，如果是自然继承就是inherit，否则和initial一样

